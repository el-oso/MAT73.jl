import{_ as t,o as a,c as r,ao as o}from"./chunks/framework.PjC_-b_z.js";const p=JSON.parse('{"title":"The format","description":"","frontmatter":{},"headers":[],"relativePath":"format.md","filePath":"format.md","lastUpdated":null}'),s={name:"format.md"};function i(d,e,n,h,l,c){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="The-format" tabindex="-1">The format <a class="header-anchor" href="#The-format" aria-label="Permalink to &quot;The format {#The-format}&quot;">​</a></h1><p>A <code>.mat</code> file written by <code>save -v7.3</code> is an HDF5 file with a MATLAB banner in front of it and a handful of <code>MATLAB_*</code> attributes beside each dataset. HDF5 says how the bytes are stored; those attributes say what they mean.</p><h2 id="What-is-read" tabindex="-1">What is read <a class="header-anchor" href="#What-is-read" aria-label="Permalink to &quot;What is read {#What-is-read}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">MATLAB class</th><th style="text-align:right;">Julia type</th></tr></thead><tbody><tr><td style="text-align:right;"><code>double</code>, <code>single</code></td><td style="text-align:right;"><code>Array{Float64,N}</code>, <code>Array{Float32,N}</code></td></tr><tr><td style="text-align:right;"><code>int8</code>–<code>int64</code>, <code>uint8</code>–<code>uint64</code></td><td style="text-align:right;"><code>Array{T,N}</code></td></tr><tr><td style="text-align:right;"><code>logical</code></td><td style="text-align:right;"><code>Array{Bool,N}</code></td></tr><tr><td style="text-align:right;">complex numeric</td><td style="text-align:right;"><code>Array{Complex{T},N}</code></td></tr><tr><td style="text-align:right;"><code>char</code>, <code>1xN</code></td><td style="text-align:right;"><code>String</code></td></tr><tr><td style="text-align:right;">empty arrays</td><td style="text-align:right;">shape preserved, so a <code>0x3</code> stays <code>0x3</code></td></tr></tbody></table><p>An empty array is not stored as zero elements. MATLAB writes a <code>uint64</code> vector holding the dimensions, tagged with <code>MATLAB_empty</code>, which is how the shape survives.</p><p>Two attributes do the work that HDF5 cannot: <code>MATLAB_class</code> names the class, and <code>MATLAB_int_decode</code> distinguishes <code>logical</code> (1) and <code>char</code> (2) from the plain integers they are otherwise indistinguishable from. Reading a logical array as <code>UInt8</code> is an error rather than a silent success, and vice versa.</p><h2 id="Storage" tabindex="-1">Storage <a class="header-anchor" href="#Storage" aria-label="Permalink to &quot;Storage {#Storage}&quot;">​</a></h2><p>Both shapes of the format are read:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">MATLAB writes</th><th style="text-align:right;">libhdf5 writes</th></tr></thead><tbody><tr><td style="text-align:right;">Superblock</td><td style="text-align:right;">version 0</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Object headers</td><td style="text-align:right;">version 1</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Groups</td><td style="text-align:right;">local heap + version-1 B-tree</td><td style="text-align:right;">link messages</td></tr></tbody></table><p>MATLAB produces the first column. libhdf5 produces the second, and so do HDF5.jl, MAT.jl and this package&#39;s own writer — MATLAB reads both.</p><p>Data layout: compact, contiguous and chunked, the last with the deflate and shuffle filters. This matters more than it looks. MATLAB compresses anything beyond a few hundred bytes, so chunked storage is the path essentially every real array takes; only the smallest variables are stored compact.</p><h2 id="What-is-written" tabindex="-1">What is written <a class="header-anchor" href="#What-is-written" aria-label="Permalink to &quot;What is written {#What-is-written}&quot;">​</a></h2><p><code>matwrite</code> emits the libhdf5 shape: a 512-byte user block holding the MATLAB banner, superblock version 2, version-2 object headers, a root group of link messages, and contiguous uncompressed datasets carrying the <code>MATLAB_*</code> attributes.</p><p>The version-2 structures each carry a Jenkins lookup3 checksum. libhdf5 verifies them, so a wrong checksum is rejected outright rather than tolerated.</p><p>Numeric arrays, <code>Bool</code> and strings are written. Compression on write is not implemented, so files are larger than MATLAB&#39;s own.</p><h2 id="What-is-not-handled" tabindex="-1">What is not handled <a class="header-anchor" href="#What-is-not-handled" aria-label="Permalink to &quot;What is not handled {#What-is-not-handled}&quot;">​</a></h2><p>Each of these raises an error naming what is unsupported, or reports <code>MAT_UNSUPPORTED</code>.</p><ul><li><p><strong>Cell arrays</strong> and <strong>structs</strong>, including struct arrays. Both need traversal of non-root groups and dereferencing of HDF5 object references into <code>#refs#</code>; structs additionally need variable-length string attributes, which live in the global heap.</p></li><li><p><strong>Sparse arrays.</strong> When implemented, <code>SparseArrays</code> will be a weak dependency: it pulls <code>SuiteSparse_jll</code>, an artifact JLL whose <code>__init__</code> aborts a trimmed binary before <code>main</code> when the depot is unreachable, so it must stay off the default path.</p></li><li><p><strong>MATLAB objects</strong> — <code>classdef</code> instances, <code>table</code>, <code>datetime</code>, <code>string</code> arrays and function handles. These live in <code>#subsystem#</code> in MATLAB&#39;s own MCOS encoding.</p></li><li><p><strong>Char matrices.</strong> Only a <code>1xN</code> char array has a single string form; a char matrix is several rows and is refused rather than flattened.</p></li></ul><p>Storage not read: fractal-heap groups, superblock versions 1 and 3, and filters other than deflate and shuffle. MATLAB writes none of these, though <code>h5repack</code> and other HDF5 writers do.</p><p>Rank is capped at 8 dimensions and a filter pipeline at 4 entries, both far above anything MATLAB produces.</p><h2 id="Testing" tabindex="-1">Testing <a class="header-anchor" href="#Testing" aria-label="Permalink to &quot;Testing {#Testing}&quot;">​</a></h2><p>Values are compared against MAT.jl, which reads through libhdf5 and is therefore an oracle independent of this implementation, over fixtures MATLAB itself wrote. Written files are read back through libhdf5 for the same reason — it verifies the checksums and the structure, so a malformed file fails there rather than passing a self-consistent round-trip.</p>',22)])])}const u=t(s,[["render",i]]);export{p as __pageData,u as default};
