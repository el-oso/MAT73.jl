import{_ as t,o as a,c as s,ao as i}from"./chunks/framework.PjC_-b_z.js";const g=JSON.parse('{"title":"The format","description":"","frontmatter":{},"headers":[],"relativePath":"format.md","filePath":"format.md","lastUpdated":null}'),r={name:"format.md"};function o(n,e,d,l,h,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="The-format" tabindex="-1">The format <a class="header-anchor" href="#The-format" aria-label="Permalink to &quot;The format {#The-format}&quot;">​</a></h1><p>A <code>.mat</code> file written by <code>save -v7.3</code> is an HDF5 file with a MATLAB banner in front of it and a handful of <code>MATLAB_*</code> attributes beside each dataset. HDF5 says how the bytes are stored; those attributes say what they mean.</p><h2 id="What-is-read" tabindex="-1">What is read <a class="header-anchor" href="#What-is-read" aria-label="Permalink to &quot;What is read {#What-is-read}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">MATLAB class</th><th style="text-align:right;">Julia type</th></tr></thead><tbody><tr><td style="text-align:right;"><code>double</code>, <code>single</code></td><td style="text-align:right;"><code>Array{Float64,N}</code>, <code>Array{Float32,N}</code></td></tr><tr><td style="text-align:right;"><code>int8</code>–<code>int64</code>, <code>uint8</code>–<code>uint64</code></td><td style="text-align:right;"><code>Array{T,N}</code></td></tr><tr><td style="text-align:right;"><code>logical</code></td><td style="text-align:right;"><code>Array{Bool,N}</code></td></tr><tr><td style="text-align:right;">complex numeric</td><td style="text-align:right;"><code>Array{Complex{T},N}</code></td></tr><tr><td style="text-align:right;"><code>char</code>, <code>1xN</code></td><td style="text-align:right;"><code>String</code></td></tr><tr><td style="text-align:right;"><code>char</code>, any shape</td><td style="text-align:right;"><code>Array{Char,N}</code>, UTF-16 code units</td></tr><tr><td style="text-align:right;">empty arrays</td><td style="text-align:right;">shape preserved, so a <code>0x3</code> stays <code>0x3</code></td></tr><tr><td style="text-align:right;"><code>cell</code></td><td style="text-align:right;"><code>Array{MatRef,N}</code>, one reference per element</td></tr><tr><td style="text-align:right;"><code>struct</code></td><td style="text-align:right;">a group; fields by path, <code>matkeys</code> lists them</td></tr><tr><td style="text-align:right;">struct arrays</td><td style="text-align:right;">fields are <code>Array{MatRef,N}</code>, one reference per element</td></tr></tbody></table><h2 id="Containers" tabindex="-1">Containers <a class="header-anchor" href="#Containers" aria-label="Permalink to &quot;Containers {#Containers}&quot;">​</a></h2><p>A cell array&#39;s elements have no common type, and neither do a struct array&#39;s, so following them eagerly would mean returning <code>Any</code>. Instead they come back as <a href="/MAT73.jl/dev/api#MAT73.MatRef"><code>MatRef</code></a> values, and you read each one with the type you expect — the same discipline as everywhere else:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cells </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;c&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{MatRef})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matclass</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, cells[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># survey before committing to a type</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, cells[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], Matrix{Float64})      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># follow it</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matkeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)                            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># field names of a struct</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;s/a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Matrix{Float64})         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a field by path, nested paths allowed</span></span></code></pre></div><p>Nesting works the same way: a cell inside a cell yields another <code>Array{MatRef}</code>.</p><p>An empty array is not stored as zero elements. MATLAB writes a <code>uint64</code> vector holding the dimensions, tagged with <code>MATLAB_empty</code>, which is how the shape survives.</p><p>Two attributes do the work that HDF5 cannot: <code>MATLAB_class</code> names the class, and <code>MATLAB_int_decode</code> distinguishes <code>logical</code> (1) and <code>char</code> (2) from the plain integers they are otherwise indistinguishable from. Reading a logical array as <code>UInt8</code> is an error rather than a silent success, and vice versa.</p><h2 id="Storage" tabindex="-1">Storage <a class="header-anchor" href="#Storage" aria-label="Permalink to &quot;Storage {#Storage}&quot;">​</a></h2><p>Both shapes of the format are read:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">MATLAB writes</th><th style="text-align:right;">libhdf5 writes</th></tr></thead><tbody><tr><td style="text-align:right;">Superblock</td><td style="text-align:right;">version 0</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Object headers</td><td style="text-align:right;">version 1</td><td style="text-align:right;">version 2</td></tr><tr><td style="text-align:right;">Groups</td><td style="text-align:right;">local heap + version-1 B-tree</td><td style="text-align:right;">link messages</td></tr></tbody></table><p>MATLAB produces the first column. libhdf5 produces the second, and so do HDF5.jl, MAT.jl and this package&#39;s own writer — MATLAB reads both.</p><p>Data layout: compact, contiguous and chunked, the last with the deflate and shuffle filters. This matters more than it looks. MATLAB compresses anything beyond a few hundred bytes, so chunked storage is the path essentially every real array takes; only the smallest variables are stored compact.</p><h2 id="What-is-written" tabindex="-1">What is written <a class="header-anchor" href="#What-is-written" aria-label="Permalink to &quot;What is written {#What-is-written}&quot;">​</a></h2><p><code>matwrite</code> emits the libhdf5 shape: a 512-byte user block holding the MATLAB banner, superblock version 2, version-2 object headers, a root group of link messages, and contiguous uncompressed datasets carrying the <code>MATLAB_*</code> attributes.</p><p>The version-2 structures each carry a Jenkins lookup3 checksum. libhdf5 verifies them, so a wrong checksum is rejected outright rather than tolerated.</p><p>Numeric arrays, <code>Bool</code> and strings are written. Compression on write is not implemented, so files are larger than MATLAB&#39;s own.</p><h2 id="What-is-not-handled" tabindex="-1">What is not handled <a class="header-anchor" href="#What-is-not-handled" aria-label="Permalink to &quot;What is not handled {#What-is-not-handled}&quot;">​</a></h2><p>One limitation is silent, so it comes first:</p><ul><li><strong>Struct field order.</strong> <code>matkeys</code> lists fields in the order the group stores them, which is not necessarily MATLAB&#39;s. Nothing raises; the names and values are right, the order may not be. MATLAB&#39;s order lives in the <code>MATLAB_fields</code> attribute, a variable-length string array that needs the global heap. Sort the names yourself if you depend on the order.</li></ul><p>The rest raise an error naming what is unsupported, or report <code>MAT_UNSUPPORTED</code>.</p><ul><li><p><strong>Sparse arrays.</strong> When implemented, <code>SparseArrays</code> will be a weak dependency: it pulls <code>SuiteSparse_jll</code>, an artifact JLL whose <code>__init__</code> aborts a trimmed binary before <code>main</code> when the depot is unreachable, so it must stay off the default path.</p></li><li><p><strong>MATLAB objects</strong> — <code>classdef</code> instances, <code>table</code>, <code>datetime</code>, <code>string</code> arrays and function handles. These live in <code>#subsystem#</code> in MATLAB&#39;s own MCOS encoding.</p></li><li><p><strong>Characters outside the basic multilingual plane.</strong> MATLAB stores char data as UTF-16 code units, and <code>Array{Char,N}</code> returns them one for one, so an astral character comes back as its two surrogates. The <code>String</code> method decodes properly. MAT.jl instead decodes a char matrix to one <code>String</code> per row; this package returns what MATLAB stores.</p></li></ul><p>Storage not read: fractal-heap groups, superblock versions 1 and 3, and filters other than deflate and shuffle. MATLAB writes none of these, though <code>h5repack</code> and other HDF5 writers do.</p><p>Rank is capped at 8 dimensions and a filter pipeline at 4 entries, both far above anything MATLAB produces.</p><h2 id="Testing" tabindex="-1">Testing <a class="header-anchor" href="#Testing" aria-label="Permalink to &quot;Testing {#Testing}&quot;">​</a></h2><p>Values are compared against MAT.jl, which reads through libhdf5 and is therefore an oracle independent of this implementation, over fixtures MATLAB itself wrote. Written files are read back through libhdf5 for the same reason — it verifies the checksums and the structure, so a malformed file fails there rather than passing a self-consistent round-trip.</p>`,28)])])}const y=t(r,[["render",o]]);export{g as __pageData,y as default};
