import{_ as a,o as e,c as s,ao as t}from"./chunks/framework.PjC_-b_z.js";const k=JSON.parse('{"title":"Trimmed binaries","description":"","frontmatter":{},"headers":[],"relativePath":"trim.md","filePath":"trim.md","lastUpdated":null}'),n={name:"trim.md"};function l(h,i,o,r,d,p){return e(),s("div",null,[...i[0]||(i[0]=[t(`<h1 id="Trimmed-binaries" tabindex="-1">Trimmed binaries <a class="header-anchor" href="#Trimmed-binaries" aria-label="Permalink to &quot;Trimmed binaries {#Trimmed-binaries}&quot;">​</a></h1><p><code>juliac --trim=safe</code> compiles a program to a small native binary by discarding everything it cannot prove is reachable. It refuses to build if any call cannot be resolved statically, which rules out most of what a dynamic file reader normally does. MAT73.jl is written to survive it, and that constraint shapes the API.</p><h2 id="Using-it" tabindex="-1">Using it <a class="header-anchor" href="#Using-it" aria-label="Permalink to &quot;Using it {#Using-it}&quot;">​</a></h2><p>The entry point must define <code>@main</code> at top level — <code>juliac</code> includes the file into <code>Main</code>, and a module wrapper is only allowed for a package entry:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MAT73</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)(args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{String}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Cint</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Core</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">stdout</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                     # not Base.stdout, which is an abstract global</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> matread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">matopen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]), args[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(out, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;n=&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot; sum=&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Cint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>julia --project=. \\</span></span>
<span class="line"><span>  $(julia -e &#39;print(joinpath(Sys.BINDIR, &quot;..&quot;, &quot;share&quot;, &quot;julia&quot;, &quot;juliac&quot;, &quot;juliac.jl&quot;))&#39;) \\</span></span>
<span class="line"><span>  --output-exe matsum --experimental --trim=safe entry.jl &lt; /dev/null</span></span></code></pre></div><p>Redirecting stdin is not cosmetic: without it the juliaup launcher can panic reading a non-blocking stdin <em>after</em> the build has already succeeded, which reads as a build failure.</p><p>The repository&#39;s <code>juliac/build.jl</code> does all of this and then checks the binary&#39;s output against the same read performed in ordinary Julia.</p><h2 id="What-it-costs-the-API" tabindex="-1">What it costs the API <a class="header-anchor" href="#What-it-costs-the-API" aria-label="Permalink to &quot;What it costs the API {#What-it-costs-the-API}&quot;">​</a></h2><p><code>--trim=safe</code> forbids a call whose return type is not statically known, which is why <a href="/MAT73.jl/dev/api#MAT73.matread"><code>matread</code></a> takes the type as an argument rather than inferring it from the file. It is also why <a href="/MAT73.jl/dev/api#MAT73.matsize"><code>matsize</code></a> returns a <code>Vector{Int}</code> and not a tuple: the rank comes from the file, so no tuple length could be concrete.</p><p>Two internal consequences are worth knowing if you read the source. The filter pipeline is a fixed-size tuple of filter ids rather than a vector of filter objects — a container whose length or element type comes from the file cannot be dispatched on statically. And no helper closes over a local that is assigned in a branch: such a local is boxed, the box reads infer <code>Any</code>, and the enclosing function stops resolving.</p><h2 id="The-two-gates" tabindex="-1">The two gates <a class="header-anchor" href="#The-two-gates" aria-label="Permalink to &quot;The two gates {#The-two-gates}&quot;">​</a></h2><p>Both run in CI, and they cover different things.</p><p><code>trim/trimcheck.jl</code> runs the verifier over every entry point. It calls the same compiler pass a real build does — <code>typeinf_ext_toplevel</code> under <code>TRIM_SAFE</code>, with the same <code>juliac-trim-base.jl</code> patches — so it reports the same errors, finalizers included. A failure there is a failure of the build. It lives outside the package test environment because TrimCheck needs the <code>Compiler</code> stdlib from Julia 1.12, while the package itself supports 1.10.</p><p><code>juliac/build.jl</code> builds an actual binary and runs it. That adds what the verifier cannot do: it links, it starts up, and it roots <code>@main</code> plus every loaded package&#39;s <code>__init__</code> rather than one signature. Both gaps have produced real failures in code that verified clean — an artifact-backed JLL aborting in <code>__init__</code> before <code>main</code>, and a <code>ccall</code> whose library operand is module-qualified, which verifies and then throws at run time.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>julia --project=trim -e &#39;using Pkg; Pkg.develop(path=&quot;.&quot;); Pkg.instantiate()&#39;</span></span>
<span class="line"><span>julia --project=trim trim/trimcheck.jl</span></span>
<span class="line"><span>julia --project=. juliac/build.jl</span></span></code></pre></div><h2 id="Dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#Dependencies" aria-label="Permalink to &quot;Dependencies {#Dependencies}&quot;">​</a></h2><p>The package depends on <code>Mmap</code> and <code>ChunkCodecLibZlib</code>. The latter <code>ccall</code>s into <code>Zlib_jll</code>, which on Julia 1.12 is a stdlib stub declaring <code>libz.so.1</code> as a compile-time constant with no artifact behind it. A trimmed binary therefore resolves zlib through the runpath <code>juliac</code> already bakes in, and gains no external dependency — <code>libz.so.1</code> does not even appear in the binary&#39;s <code>DT_NEEDED</code>.</p><p>That is the reason to be careful about adding dependencies here. An artifact-backed JLL resolves its artifact at run time from <code>DEPOT_PATH</code>, and a trimmed binary that cannot find it aborts before <code>main</code> rather than failing at the call site.</p>`,19)])])}const u=a(n,[["render",l]]);export{k as __pageData,u as default};
